```git add filename``` or ```git add *``` to stage changes. 

```git commit``` opens ```vim``` editor for detailed message. 

```git push origin main``` to push the changes to the remote repository. 
